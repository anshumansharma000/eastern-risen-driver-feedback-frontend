import Link from "next/link";
import { Unavailable } from "@/components/ui";
export default function AdminPage(){const links=[["/admin/trips","Trips","Create, assign, edit, and archive journeys."],["/admin/drivers","Drivers","Manage agency and outsourced driver accounts."],["/admin/vehicles","Vehicles","Maintain active fleet records."],["/admin/vendors","Vendors","Manage outsourced-driver companies."],["/admin/questionnaires","Questionnaires","Draft, version, and publish the passenger form."],["/admin/consent","Consent","Review or activate the passenger privacy notice."]] as const;return <main className="page"><div className="page-header"><div><p className="eyebrow">Eastern Risen operations</p><h1>Keep journeys moving.</h1><p>Use the supported operational modules below. Analytics and passenger feedback review remain gated until their backend contracts exist.</p></div></div><section className="grid-3" style={{marginBottom:"1rem"}}>{links.map(([href,title,copy],index)=><Link href={href} className="card card-pad" key={href}><span className="eyebrow">0{index+1}</span><h2 className="section-title">{title}</h2><p>{copy}</p><strong style={{color:"var(--primary)"}}>Open {title.toLowerCase()} →</strong></Link>)}</section><div className="grid-2"><Unavailable title="No invented dashboard metrics">Response counts, rating trends, and negative-feedback alerts will appear only after analytics endpoints define their calculation and data.</Unavailable><Unavailable title="No passenger records in this build">Feedback review, flagging, and archiving require the missing admin feedback API.</Unavailable></div></main>}
