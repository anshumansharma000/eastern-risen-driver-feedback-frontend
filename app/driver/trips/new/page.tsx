import Link from "next/link"; import { Unavailable } from "@/components/ui"; export default function NewTripPage(){return <main className="page"><div className="page-header"><div><p className="eyebrow">Driver-entered journey</p><h1>Enter a trip</h1><p>A manual trip is always assigned to the signed-in driver.</p></div></div><Unavailable title="Vehicle selection needs one API addition">The trip-create endpoint requires an active vehicle ID, but the current driver API does not expose an active-vehicle list. This form is gated so we do not ask drivers to paste internal identifiers or simulate a successful trip. An administrator can create and assign the trip now.</Unavailable><p style={{marginTop:"1rem"}}><Link className="button button-secondary" href="/driver">Back to journeys</Link></p></main>}
