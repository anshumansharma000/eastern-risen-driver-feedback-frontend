import Link from "next/link"; import { Unavailable } from "@/components/ui"; export default function NewTripPage(){return <main className="page"><div className="page-header"><div><p className="eyebrow">Driver-entered journey</p><h1>Enter a trip</h1><p>A manual trip is always assigned to the signed-in driver.</p></div></div><Unavailable title="Booking and vehicle selection need driver APIs">The trip-create contract now requires an active booking ID and vehicle ID, but the driver API does not expose lists for either resource. This form remains gated so drivers are never asked to paste internal identifiers. An administrator can add the trip from the booking detail page now.</Unavailable><p style={{marginTop:"1rem"}}><Link className="button button-secondary" href="/driver">Back to journeys</Link></p></main>}
