"use client";
import { FormEvent,useEffect,useState } from "react";
import type { ConsentVersion,DataResponse } from "@/lib/contracts";
import { ApiError,apiRequest,errorMessage } from "@/lib/api";
import { ErrorAlert,LoadingCards,StatusBadge } from "./ui";
export function ConsentManager(){const [consent,setConsent]=useState<ConsentVersion|null>(null);const [error,setError]=useState<{message:string;requestId?:string}|null>(null);const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);async function load(){try{const r=await apiRequest<DataResponse<ConsentVersion>>("/api/v1/admin/consent-versions/active");setConsent(r.data)}catch(cause){setError({message:errorMessage(cause),requestId:cause instanceof ApiError?cause.requestId:undefined})}}useEffect(()=>{queueMicrotask(()=>void load())},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!confirm("Activate this consent notice? The current version will be retired and the new text will be immutable."))return;setBusy(true);const d=new FormData(e.currentTarget);try{await apiRequest("/api/v1/admin/consent-versions",{method:"POST",body:JSON.stringify({content:d.get("content")})});setShow(false);await load()}catch(cause){setError({message:errorMessage(cause),requestId:cause instanceof ApiError?cause.requestId:undefined})}finally{setBusy(false)}}return <><div className="page-header"><div><p className="eyebrow">Passenger privacy</p><h1>Consent notice</h1><p>The active notice is shown in full before a passenger can submit feedback. Activated versions are immutable.</p></div><button className="button" onClick={()=>setShow(true)}>Create new version</button></div>{error&&<ErrorAlert message={error.message} requestId={error.requestId}/>} {!consent&&!error&&<LoadingCards/>}{consent&&<section className="card card-pad"><div className="trip-card-head"><div><span className="eyebrow">Version {consent.version}</span><h2 className="section-title">Active passenger notice</h2></div><StatusBadge label="Active" tone="success"/></div><div className="consent-box" style={{maxHeight:"none"}}>{consent.content}</div><p className="trip-meta">Effective {new Intl.DateTimeFormat("en-IN",{dateStyle:"long"}).format(new Date(consent.effectiveAt))}</p></section>}{show&&<div className="dialog-backdrop"><form className="dialog" onSubmit={create}><span className="eyebrow">Immutable on activation</span><h2>New consent version</h2><div className="field"><label htmlFor="content">Full privacy and consent text</label><textarea className="textarea" id="content" name="content" required minLength={1} maxLength={20000}/></div><div className="alert alert-warning">Activating this text retires the current version. Existing feedback keeps the version each passenger accepted.</div><div className="dialog-actions"><button type="button" className="button button-secondary" onClick={()=>setShow(false)}>Cancel</button><button className="button" disabled={busy}>Review and activate</button></div></form></div>}</>}
